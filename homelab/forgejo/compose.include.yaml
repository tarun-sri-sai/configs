include:
  - ../postgres/compose.include.yaml

services:
  forgejo:
    depends_on:
      - postgres
    env_file:
      - .env
    hostname: forgejo
    image: codeberg.org/forgejo/forgejo:16
    ports:
      - "2222:22"
    restart: unless-stopped
    volumes:
      - ${FORGEJO_DATA}:/data
