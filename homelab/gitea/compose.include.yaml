include:
  - ../postgres/compose.include.yaml

services:
  gitea:
    depends_on:
      - postgres
    env_file:
      - .env
    hostname: gitea
    image: docker.io/gitea/gitea:1.26
    ports:
      - "2222:22"
    restart: unless-stopped
    volumes:
      - ${GITEA_DATA}:/data
