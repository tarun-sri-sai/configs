include:
  - ../immich/compose.include.yaml
  - ../seaweedfs/compose.include.yaml
  - ../gitea/compose.include.yaml

services:
  nginx:
    depends_on:
      - immich-server
      - s3
      - gitea
    hostname: nginx
    image: docker.io/library/nginx:1.30-alpine
    ports:
      - "3000:3000"
      - "2283:2283"
      - "8333:8333"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
