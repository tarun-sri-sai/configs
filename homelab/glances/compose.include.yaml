include:
  - ../influxdb/compose.include.yaml

services:
  glances:
    depends_on:
      - influxdb
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0"]
              capabilities: [gpu]
    env_file:
      - .env
    hostname: glances
    image: docker.io/nicolargo/glances:ubuntu-4.3.2-full@sha256:b58d312c7339acc40321b4965e73dfb41bd57b3e68eef79b29a42d16d49485c6
    network_mode: host
    pid: host
    restart: unless-stopped
    volumes:
      - /run/user/1000/podman/podman.sock:/run/user/1000/podman/podman.sock:ro
      - ./glances.conf:/etc/glances/glances.conf:ro
